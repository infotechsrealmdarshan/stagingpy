/**
 * Frontend configuration.
 * Change API_BASE to your deployed backend URL when going to production.
 * Leave as empty string '' to use the same origin (when serving frontend from backend).
 *
 * Examples:
 *   const API_BASE = '';                            // same-origin (dev with proxy)
 *   const API_BASE = 'http://localhost:5000';       // local backend
 *   const API_BASE = 'https://api.your-domain.com'; // production
 */
const API_BASE = 'https://stagingpy.onrender.com';
